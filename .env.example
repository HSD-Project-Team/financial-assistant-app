################################################################################
# Finance Assistant Monorepo — .env.example
#
# Kurulum:
#   1) Bu dosyayı kopyala:
#        Windows (PowerShell) : Copy .env.example .env
#        macOS / Linux        : cp .env.example .env
#
#   2) Aşağıdaki değerleri kendi ortamına göre doldur.
#
#   3) Staging/Render ortamında .env commitlenmez;
#      değerler Render Dashboard ve EAS Secrets üzerinden girilir.
#
# NOT:
#   - EXPO_PUBLIC_* değişkenleri mobile build/runtime'a açık olarak enjekte
#     edilir. Gizli bilgi (key, secret) ASLA bu prefix ile yazılmaz.
#   - SUPABASE_SERVICE_ROLE_KEY sadece backend tarafında kalır.
################################################################################


################################################################################
# SUPABASE (Ortak)
################################################################################

# Lokal: http://127.0.0.1:54321
# Staging: https://<project-ref>.supabase.co
SUPABASE_URL=http://127.0.0.1:54321

# Lokal key'leri görmek için:  pnpm db:start >> pnpm db:status
SUPABASE_ANON_KEY=# Authentication keys/Publishable
SUPABASE_SERVICE_ROLE_KEY=# Authentication keys/Secret


################################################################################
# BACKEND (Express)
################################################################################

# Backend'in dinleyeceği port
PORT=3000


################################################################################
# MOBILE (Expo)
################################################################################

# Mobile → Backend API base URL
#   Android Emulator  : http://10.0.2.2:3000
#   iOS Simulator     : http://localhost:3000
#   Gerçek cihaz      : http://<BILGISAYAR_IP>:3000
EXPO_PUBLIC_API_BASE_URL=http://10.0.2.2:3000


################################################################################
# AI-SERVICES (FastAPI)
################################################################################

# AI servisinin dinleyeceği port
AI_PORT=8000

# Backend → AI servis URL'i (ileride entegre edilecek)
# AI_BASE_URL=http://127.0.0.1:8000


################################################################################
# EAS / CI (Sadece EAS Cloud ve Render için — lokal .env'e yazma)
################################################################################
# Bu değerler lokal .env'e YAZILMAZ.
# EAS Secrets ve Render Dashboard üzerinden girilir:
#
# EXPO_PUBLIC_API_BASE_URL_STAGING=https://<render-backend-url>
# EXPO_PUBLIC_API_BASE_URL_PROD=https://<prod-url>
#
# APP_ENV → eas.json profilelerinden otomatik gelir (local / staging / prod)
################################################################################
